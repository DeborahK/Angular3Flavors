1) Open a command prompt in the project's root directory
2) Type: npm install
    This installs the packages as defined in the package.json file.
3) Type: npm start
    This launches tsc to compile the application and lite-server to run the application.